# Python
__pycache__/
*.py[cod]
*$py.class


# Virtual environment
.venv/
venv/
ENV/
env/

# Environment variables
.env

# IDE / Editor specific
.vscode/
.idea/
*.swp
*.swo
.spyderproject/
.spyderworkspace

# Cache files from your CacheManager (if it writes to a default location not already covered)
cache/ 
# If your cache_manager.py's `cache_dir` is "cache" at the root, this will ignore it.

# Vector Database (ChromaDB data)
vector_db/

# Large Data Files (if you don't want to commit the actual PDFs/JSONs)
# If your data/raw or data/processed folders are very large,
# or if you don't want to share the raw PDFs/JSONs in the repo, uncomment these.
data/raw/*
data/processed/*

# Test Output & Reports (Generally, these are artifacts generated by code, so often not committed)
# You might want to commit the *final* report JSONs/images as examples, but not all intermediate ones.
# Decide which of these you want to keep out of version control.
tests/
# Add other specific report files or directories from `tests/` if they are generated artifacts

# Other temporary/log files
*.log
debug_page.html
debug_page_*.html